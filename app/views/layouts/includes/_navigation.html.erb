<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <div class="container">
    <a class="navbar-brand" href="/">
      <%= image_tag "icon.png", alt: "Geyser Logo", height: "30", loading: "lazy" %>GeyserList
    </a>

    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a href="/" class="nav-link"><i class="fas fa-home"></i> Home</a>
        </li>
      </ul>

      <ul class="navbar-nav ml-md-auto">
        <% if @user.nil? %>
          <a class="nav-link" href="/login">Log in</a>
        <% else %>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
              You
            </a>
            <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
              <span class="dropdown-item-text">Logged in as
                <b><%= @user.username %></b> <%= @user.verified_icon(20) %></span>
              <hr/>
              <a class="dropdown-item" href="/profile/me"><i class="fas fa-user"></i> Profile</a>
              <hr/>
              <a class="dropdown-item" href="/logout"><i class="fas fa-door-open"></i> Log out</a>
            </div>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>